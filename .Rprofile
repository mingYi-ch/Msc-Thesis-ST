source("renv/activate.R")
setwd("/data/myi/mt-spatial-transcriptomics/")
source("/data/myi/mt-spatial-transcriptomics/renv/activate.R")

renv::activate()

# Change the renv root
Sys.setenv(RENV_PATHS_ROOT = "/scratch/myi/.cache/R/renv")
